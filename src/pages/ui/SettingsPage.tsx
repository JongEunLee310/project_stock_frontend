import { PagePlaceholder } from './PagePlaceholder'

export function SettingsPage() {
  return (
    <PagePlaceholder
      eyebrow="Settings"
      title="Settings"
      summary="Workspace preferences, notification settings, and data sync options will be managed here."
    />
  )
}
