# Claude Command: Document Decision

Assess whether the work requires an ADR, failure record, knowledge base update, or feedback loop record.

Use the templates in `docs/decisions/`, `docs/failures/`, and `docs/feedback/`. Record only decisions and failures that will help future work.
