# 로컬 리뷰 — PR #61 (Track A: Opus + Codex)

대상: `feat/48-remaining-opus` → `main`. 이슈 #48 §4 잔여 5화면 연동.
리뷰어: Claude Code (Opus). 설계: `docs/designs/48-remaining-wiring.md`.

## 판정: Approve (non-blocking 관찰 2건)

35파일 / +2899 −1328. `pnpm lint·typecheck·format:check` 통과, `TZ=UTC pnpm test` 40파일/183 통과(로컬 재현 확인).

## 정합성

- **경로 prefix 정상**: 모든 호출이 상대경로(`/signals`·`/assets`·`/alerts`·`/auth/me`·`/decision-logs`)로 `apiGet`/`apiPost`에 전달 → `VITE_API_BASE_URL`(`…/api/v1`) 단일 부착. 이전 라운드(pr-59)의 double-prefix 오진단 회귀 없음. **첫 패스에 정상**.
- **Research symbol→assetId 해소**: `GET /assets?symbol=` 선행 → `asset.id`로 detail/summary/checklist/reports/theses 호출(G6). detail은 `auth:false`(G7 Not required) 적용.
- **갭 처리**: 펀더멘털(per/peg/52w/target) nullable → 값 없으면 metric 행 제외(더미 금지). DecisionLog 404 시 로컬 폴백 + 주석. Settings 알림 mock 숨김(G11). Signal `trendSeries`/`previousStatus` 폐기, score→게이지, prices→스파크라인(G9).
- **mutation**: Alerts read/dismiss/confirm·DecisionLog 생성 후 해당 목록 `invalidateQueries`. 네이티브 다이얼로그 미사용.
- 어댑터 순수·동기, 화면은 FE 도메인/뷰 타입만 사용(DTO 미노출). 스코프 밖 변경 없음.

## 관찰 (non-blocking)

1. `signals/queries.ts` 가격 호출이 `market=NASDAQ` 하드코딩 — 비-NASDAQ 심볼은 스파크라인 누락 가능. 실패 시 숨김 처리라 치명적이지 않으나 후속에서 asset detail의 market 사용 권장.
2. Signal symbol은 `evidence.{symbol,ticker,asset_symbol}`에서만 추론 — BE가 evidence에 심볼을 안 넣으면 스파크라인 미표시. 계약상 Signal에 symbol 필드 부재에 따른 합리적 폴백이나, contract에 symbol 노출 협의 여지.

## 결론

Track A는 계약 정합성·검증을 첫 패스에 충족했고 Opus 추가 정정 패스 불요. 머지 후보로 적격.
