# PR #26 Local Review — 라우팅 구조 및 App Shell 레이아웃 (task-003 / 이슈 4·5)

## Review Summary

task-003 핸드오프 구현 리뷰. `react-router-dom`을 도입해 8개 주요 라우트와 NotFound를 레이아웃 라우트로 구성하고, 모든 페이지를 감싸는 App Shell(Sidebar/Topbar/MarketSummary/`<Outlet/>` 콘텐츠 슬롯)을 구현했다. 핸드오프 Goal·Implementation Scope·Test Requirements를 모두 충족하며, Out of Scope(페이지 실제 콘텐츠·디자인 토큰 정의·도메인 연동) 침범 없음. 보호 파일 변경 없음.

검토 항목:

- 라우트와 Sidebar 메뉴가 `shared/config/navigation.ts` 단일 소스에서 파생 — 핸드오프 "단일 소스 일관 파생" 요구 충족. `appRoutePaths`를 라우터(`router.tsx`)와 네비게이션(`navigationItems`)이 공유.
- 활성 메뉴는 `getActiveNavigationItem`이 `matchPrefix`(`/research`) 기반으로 중첩 경로(`/research/NVDA`)에서도 정확히 판정. `aria-current="page"`로 접근성 표시.
- App Shell이 레이아웃 라우트(`element: <AppShell/>` + children)로 전 라우트를 감싸고, NotFound(`path: '*'`)도 Shell 내부에 렌더 — 핸드오프 "모든 라우트를 감싸는 레이아웃 라우트" 요구 충족.
- 의존성은 `react-router-dom`만 추가(핸드오프 제약 준수). 그 외 추가 없음.
- 데스크톱 기준 `lg:grid-cols` 반응형, 토큰(`app-*`) 사용으로 task-002 토큰 재사용.

## Blocking

None.

## Suggestions

- Prettier 포맷 불일치가 13개 파일에서 발생(`prettier --check` 기준). 이 중 task-002 잔여 4건(`src/index.css`, `src/shared/ui/Card.tsx`, `src/shared/ui/Input.tsx`, `src/shared/ui/stockStatus.ts`)이 PR #25 제안 이후에도 미해결 상태로 남아 있고, task-003 신규 파일 9건이 추가로 누적됐다. 동일 원인(커밋 전 `pnpm format` 미실행)이 PR을 넘어 반복되고 있다. 동일 브랜치에서 `pnpm format` 적용 후 푸시 권장(Codex 후속).
- 반복 누적을 근본 차단하려면 CI에 `prettier --check` 단계 추가를 검토할 가치가 있다. 다만 이는 CI(보호 파일) 변경이므로 별도 사람 승인·핸드오프 사안. 본 PR 범위 밖.

## Questions

None.

## CI Result

Verify React Template — pass (28s). 로컬 검증 `pnpm lint`/`typecheck`/`test`(10 passed)/`build` 모두 통과.

## Documentation Impact

핸드오프 Documentation Impact는 선택(README 라우트 표). 미반영이나 placeholder 단계 레이아웃이라 차단 아님. 페이지 콘텐츠가 채워지는 후속 이슈(7~)에서 함께 문서화 가능. 추가 ADR·실패 기록 불필요(React Router 채택은 ADR-003에 기록됨).

## Final Recommendation

머지 가능 상태. 핸드오프 범위·검증 충족, 차단 이슈 없음. Prettier 포맷 누적은 비차단이나 PR을 넘어 반복되므로 후속 정리(가능하면 CI 강제) 권장. 자동 승인하지 않으며 최종 승인·머지는 사람이 수행한다.
