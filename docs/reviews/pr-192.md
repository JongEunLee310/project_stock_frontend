# PR #192 로컬 리뷰 — feat: 리서치 헤더 정비 — AI 스탠스 디자인 정렬·지표 2행 (#185)

리뷰어: Claude (value-for-fable / Sonnet)

구현자: Codex(gpt-5.6-sol), 커밋 `c55afe9`(설계 스켈레톤) ·
`99775ba`(구현), 이슈 #185(에픽 #152)

## Review Summary

`git diff main...HEAD` 기준 2개 파일이 바뀝니다:
`docs/designs/185-research-header-stance-metrics.md`(신규, +49) ·
`src/pages/ui/ResearchPage.tsx`(+54/-19, `HeaderCard` 함수 내부만).
설계 문서(`docs/designs/185-research-header-stance-metrics.md`)에 기술된
범위와 diff가 정확히 일치합니다. 테스트 파일 변경은 없습니다.

지표 데이터는 기존 단일 `metricTiles`(5개: 시가총액·섹터·52주 범위·
다음 실적 발표·평균 목표주가)를 `primaryMetricTiles`(시가총액·52주
범위, 2개)와 `secondaryMetricTiles`(섹터·다음 실적 발표·평균 목표주가,
3개)로 분리했습니다(761~790행). 값 계산 로직(`formatCurrency`,
`formatPercent` 호출)은 그대로 옮겨졌을 뿐 변경되지 않았고, 5개 필드
모두 diff 이후에도 그대로 렌더링됩니다. 두 `dl`은 각각 자체적으로
`dt`/`dd` 짝을 완결된 형태로 갖추므로(903~928행) 시맨틱 위반이
아닙니다 — HTML 명세상 설명 목록(description list)은 문서당 하나로
제한되지 않습니다. 각 지표 항목의 `min-w-0` 클래스(906행·918행)도
분리 전과 동일하게 두 그리드 모두에 유지되어, `grid-cols-3` 행의
'다음 실적 발표' 같은 긴 라벨·값이 그리드 트랙 폭을 초과해 형제 열을
밀어내는 오버플로를 방지합니다.

헤더 최상위 4열 grid 정의(`xl:grid-cols-[minmax(12rem,1.1fr)_
minmax(10rem,0.7fr)_minmax(12rem,0.85fr)_minmax(19rem,1.6fr)]`, 794행)는
diff에 포함되지 않아 변경되지 않았습니다. 설계 문서가 명시한 "PR #173
오버플로 회귀 방지" 제약이 실제로 지켜졌습니다.

AI 투자 스탠스 박스는 컨테이너를 `flex flex-col items-center
justify-center gap-2 ... text-center`로 바꿔 세로 중앙 정렬 구성으로
전환했고(872행), 배지에 `px-3 py-1 text-sm font-semibold` 클래스를
className override로만 추가했습니다(876행) — `Badge` 컴포넌트 자체
(`src/shared/ui/Badge.tsx`)나 다른 화면에서의 `Badge` 사용에는 영향이
없습니다. `InfoTooltip` 노출 조건 `research.confidenceBasis ||
research.stanceComment`(885행)는 변경 전후 동일한 표현식이며, 위치만
배지 바깥의 `flex-wrap` 하위 div로 이동했을 뿐 조건 로직 자체는
그대로입니다.

`ResearchPage.test.tsx`의 관련 단언(1020~1067행)은 모두
`getByText`/`getByRole` 기반으로 라벨·값 텍스트와 접근성 속성만
검증하며 DOM 구조(단일 `dl` 여부 등)를 가정하지 않으므로, 이번 구조
변경과 무관하게 그대로 유효합니다. 실제로 로컬 재실행에서도 수정 없이
통과했습니다.

## Blocking

없습니다.

## Suggestions

없습니다.

## Questions

없습니다.

## CI Result

GitHub Actions CI(`Verify React Template`)는 리뷰 시점 기준 `pass`
상태입니다(`gh pr checks 192`, 1m1s). 로컬에서도 4종 검증을 직접
재실행해 확인했습니다.

- `pnpm format:check` — `All matched files use Prettier code style!` 통과
- `pnpm typecheck` — 에러 없이 통과
- `pnpm lint` — 에러 없이 통과
- `pnpm test -- --run` — 59개 파일, 496개 테스트 전부 통과 (`Test Files 59
  passed (59)`, `Tests 496 passed (496)`)

오케스트레이터가 사전에 재실행한 결과(59파일 496개 통과) 및 PR 본문의
검증 기록과 일치합니다.

## Documentation Impact

설계 문서 `docs/designs/185-research-header-stance-metrics.md`가 이번
PR에 이미 포함되어 있고, 구현이 그 범위(스탠스 박스 세로 중앙 정렬·
지표 2행 재배치·4열 grid 열 정의 유지)와 정확히 일치합니다. API
응답 계약이나 `ResearchView` 타입 변경은 없으므로 ADR 대상이
아닙니다. 실패 기록(failure record) 대상도 아닙니다. 최고·최저 목표
주가 병기는 설계 문서에 명시된 대로 BE 컨센서스 계약
(project_stock#295) 확정 후 후속 과제로 남아 있습니다.

## Final Recommendation

Blocking이 없습니다. 머지 가능 판단입니다.

설계 문서 범위와 diff가 정확히 일치하고, 지표 분리 과정에서 5개
필드·`min-w-0` 오버플로 방지·4열 grid 열 정의가 모두 보존되었습니다.
스탠스 박스의 `InfoTooltip` 노출 조건도 변경 없이 유지됩니다. 기존
텍스트 기반 테스트 단언이 구조 변경과 무관하게 그대로 통과하며, CI
(`Verify React Template`)는 `pass`이고 로컬 4종 검증(format:check·
typecheck·lint·test 496개)도 모두 통과했습니다.

최종 머지 권한은 개발자에게 있습니다.
