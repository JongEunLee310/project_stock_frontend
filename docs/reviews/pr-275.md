# PR #275 로컬 리뷰 — 토픽 인사이트 상세 화면 (#203)

## Review Summary

에픽 #198 1차의 마지막 화면인 토픽 인사이트 상세(`/news/topics/:topicId`, 분석 현미경)를 구현한 PR입니다. 커밋은 구현 본체(`feat: 토픽 인사이트 상세 화면 구현`)와 핸드오프 문서(`docs: #203 토픽 상세 핸드오프 태스크`) 두 건입니다. 개요(관제탑)에서 토픽을 클릭해 들어와 근거·영향·반대 관점을 검증하는 상·하위 관계의 하위 화면입니다.

기존 자산을 폭넓게 재사용합니다. HTTP·envelope·cursor 페이징은 `@/shared/api`(`apiGet`·`ApiCursorMeta`·`buildCursorSearchParams`·`toCursorPageInfo`)를, 차트는 `@/shared/ui/charts`의 `chartTheme`·`useMeasuredChartWidth`·`DonutChart`를, enum 라벨·톤 매핑은 `features/news-insights/adapters.ts`의 기존 presentation 맵(`sentimentPresentations`·`documentTypePresentations`) 컨벤션을 그대로 확장했습니다. 라우트 `appRoutePaths.newsTopicDetail`은 이미 정의돼 있어 `router.tsx`에 페이지만 배선했습니다.

새로 추가된 것은 features의 상세 3종 query(`useNewsTopicDetailQuery`·`useNewsTopicTrendQuery`·`useNewsTopicEvidenceQuery`)와 dto·adapter, 위젯 4종(`TopicSummaryHeader`·`TopicTrendChart`·`TopicEvidenceList`·`CounterViewPanel`), 페이지 `TopicInsightDetailPage`입니다. 세 패널은 각각 독립 query로 연동하며 한 API 오류가 페이지 전체를 깨지 않도록 부분 실패를 격리합니다. 계약 변경은 없고 BE의 dev 머지 계약(`GET /topics/{id}`·`/trend`·`/evidence`)을 소비만 합니다.

이번 리뷰는 설계 원칙 준수(점수 오인 방지·반대 관점 대칭·사실/분석 분리·수치 창작 금지)와 부분 실패 격리, 범위 통제를 축으로 봤습니다.

**라운드2 추가(`feat: 토픽 인사이트 상세 셸 정합 및 재배치`).** 디자인 정본(`topic-insight.png`)이 1·2·3차를 합친 3열 대시보드인데 라운드1은 1차 라이브 3패널만 세로 스택으로 배치해 격차가 컸습니다. 개요 셸(#199)과 동일한 방식으로 전체 대시보드 그리드를 갖추되 1차는 라이브, 2·3차는 placeholder로 재배치했습니다. 헤더를 디자인 배치로 재구성했고(아이콘·제목·태그, 종합 영향도 `/100`·감성 방향+화살표·신뢰도 `%` stat 3종, 영향 종목 칩, 우상단 액션 버튼, 하단 AI 요약 한 줄), 인사이트 요약을 `TopicInsightSummary`로, 반대 관점을 독립 `CounterViewPanel`로 분리해 각각 자체 loading·error·empty를 처리하도록 했습니다. 개요의 `PlannedPanelCard`를 공용 위젯으로 추출해 양 페이지가 공유합니다. 감성 방향(상승·중립·하락)은 `scores.sentiment` 점수 임계로 파생하며 새 데이터를 만들지 않습니다. 2·3차 패널은 데이터 없이 단계·이슈 안내만 노출합니다.

## Blocking

None.

## Suggestions

**S1. 추이 window·interval 하드코딩 + 30/90일 토글 비활성 — 현재 범위에서 허용, 후속 확장 여지.** `useNewsTopicTrendQuery`가 `window=7d&interval=1d`를 고정합니다. 라운드2에서 디자인의 7/30/90일 토글을 시각적으로 추가했으나 현재 데이터가 7일 고정이라 30/90일은 비활성(`준비 중`)입니다. 없는 데이터를 만들지 않는 처리로 적절하며, BE window 파라미터화는 후속 이슈에서 다루면 됩니다.

**S3. 감성 방향의 점수 임계 파생 — 표현 로직으로 허용.** `sentimentScorePresentation`이 `scores.sentiment`(0~1)를 50% 기준으로 상승/중립/하락으로 파생합니다. BE가 방향 enum을 따로 주지 않는 상황에서 기존 점수를 해석해 표시하는 표현 로직이며 새 수치를 만들지 않습니다. 향후 BE가 감성 방향을 명시 필드로 제공하면 그 값으로 대체하는 편이 낫습니다.

**S2. 원문 URL 부재의 정직한 노출 — 계약 한계에 대한 적절한 처리.** 근거 항목의 '원문 보기'는 API가 원문 URL·본문을 제공하지 않아 출처 메타데이터(출처·발행 시각·문서 번호)만 표시하고 그 사실을 문구로 밝힙니다. 없는 정보를 만들지 않는다는 프로젝트 원칙에 부합합니다. 원문 링크 계약이 생기면 이 영역을 연결하면 됩니다.

## Questions

**Q1. `key_evidence` 항목 구조의 안정성.** BE `TopicInsightResponse.key_evidence`는 `list[dict[str, Any]]`로 스키마가 느슨합니다. adapter의 `adaptKeyEvidence`가 `title`·`summary`·`quote`·`text`를 순서대로 탐색하고 모두 없으면 `이벤트 #{event_id}` 또는 인덱스 라벨로 폴백하도록 방어적으로 처리해 해소했습니다. 실제 시드·수집 데이터의 dict 키가 확정되면 라벨 우선순위를 재확인하면 됩니다.

## CI Result

로컬 검증은 리뷰어(Claude Code)가 2026-07-22 재실행했습니다. Codex 보고와 일치합니다.

- `pnpm format:check` — 통과.
- `pnpm lint` — 통과.
- `pnpm typecheck` — `tsc --noEmit` 오류 없음.
- `pnpm test` — 라운드2 반영 후 96 files, 754 tests 통과(라운드1 94/752 → `TopicInsightSummary`·`PlannedPanelCard` 테스트 추가).
- `pnpm build` — 통과(기존 500kB 초과 번들 경고는 이번 PR 이전부터 있던 것으로 신규 회귀 아님).

GitHub Actions(Verify React Template) — 라운드1 pass 1m46s(run 29886511787). 라운드2 재실행 결과는 push 후 확인.

## Documentation Impact

설계문서 `docs/designs/198-news-insights.md`의 화면-API 매핑(토픽 상세 3행)과 구현이 일치하므로 추가 갱신이 필요 없습니다. 핸드오프 `.codex/tasks/task-203-topic-insight-detail.md`는 이번 PR에 포함됐습니다. ADR은 불필요합니다. 기존 FSD·query·차트 패턴을 따르는 신규 화면 추가로 새로운 방향 선택이 아닙니다. Failure Record도 해당 사항이 없습니다.

## Final Recommendation

머지 가능합니다. 핸드오프 요구사항을 항목별로 확인했습니다. 세 패널 독립 query와 부분 실패 격리(`TopicSummaryHeader`·`TopicTrendChart`·`TopicEvidenceList`가 각각 loading·error·empty를 자체 처리), 점수 오인 방지 안내('관찰 우선순위'·감성 방향 안내 문구), 반대 관점 상시 노출(`CounterViewPanel`이 빈 배열에도 동일 비중 EmptyState 렌더), 사실/분석 분리(원문 정보 대 AI 요약 시각 구분), 영향 종목 → `/research/:symbol` 이동, BE 계산값 그대로 렌더가 모두 확인됩니다.

범위는 지켜졌습니다. 2·3차 패널(종목 민감도·키워드 관계망·시나리오·Agent 설명·액션 연결)은 라운드2에서도 placeholder로만 자리를 확보했고 실제 연동은 하지 않았습니다. 이벤트 상세(#204)에는 손대지 않았습니다. Protected Files는 없으며 diff는 features·widgets·pages 신규 추가와 헤더·페이지 재배치, 개요의 `PlannedPanelCard` 공용화에 한정됩니다. 신규 npm 의존성 추가도 없습니다. 추가된 테스트는 상세 3종 query·adapter(성공·오류·빈 데이터), 위젯(요약 헤더·추이·근거·반대 관점·인사이트 요약·공용 placeholder), 페이지의 패널 조합·부분 실패·placeholder 노출을 덮습니다.

라운드2의 개요 페이지 회귀는 `PlannedPanelCard` 공용화로 한정되며 표기·동작을 동일하게 유지해 개요 테스트가 통과합니다.

Blocking은 없습니다. S1·S2·S3는 후속·계약 한계·표현 로직 사항으로 병합을 막지 않으며, Q1은 리뷰 중 adapter 방어 처리로 해소했습니다.

최종 머지 권한은 개발자에게 있습니다.
