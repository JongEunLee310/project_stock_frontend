# PR #157 로컬 리뷰 — feat: 뉴스와 공시를 분리해 표시한다

리뷰어: Claude Code (Fable 5) — 교차 모델 독립 리뷰. 구현에 관여하지 않음.
(VFF 세션 한도로 이번 리뷰는 오케스트레이터가 직접 수행했습니다.)

구현자: Codex(gpt-5.6-sol, effort high), 커밋 `7c41a79`

## Review Summary

이슈 #146의 구현입니다. 리서치 상세의 "뉴스 및 공시 요약" 카드를 BE
`GET /assets/{asset_id}/news-disclosure` 계약(project_stock PR #273)에
연결합니다. 변경은 9개 파일, +526/-91 줄입니다.

- **카드 교체** — 뉴스(기본)/공시 탭으로 분리하고, 항목 행을 분류 배지 ·
  제목(원문 새 탭 링크) · 출처·게시 시각 · AI 요약 · 영향(sentiment
  tone)·중요도 라벨로 구성합니다. deep-link 앵커 `research-section-news`는
  유지되어 PR #156의 섹션 직행이 회귀하지 않습니다.
- **독립 쿼리** — `useNewsDisclosure(assetId)`가 `useResearchView`와 분리된
  쿼리로 동작해, 이 카드의 실패·빈 상태가 페이지 나머지 렌더를 막지
  않습니다 (카드 내 ErrorState·재시도·탭별 EmptyState).
- **정규화** — `mapKnownLabel`·`normalizeSentiment`가 소문자 입력을 대문자
  정규화 후 매핑하고 알 수 없는 값은 null로 떨어뜨립니다 (BE에 소문자
  `positive`/`medium` 데이터가 실재하는 점을 반영).
- **reports 정리** — 임시 소스였던 reports의 유일 소비처가 이 카드였음을
  확인하고 `ReportDto`·`adaptReport`·`ResearchView.reports`·`/reports`
  병렬 호출을 함께 제거했습니다. grep 기준 비테스트 코드에 잔재가
  없습니다.

## Blocking

없습니다.

## Suggestions

**S1. 핵심 리스크 evidence 불릿의 내용 문자열 key가 남아 있습니다**

`ResearchPage.tsx` 449행의 `<li key={evidence}>`는 PR #155에서 지적된
S3(내용 문자열 key) 패턴의 잔존 사례입니다. 이번 PR의 신규 목록은 id·url
기반 key를 썼으므로 이 PR의 결함은 아니고, #155 후속 정리 대상으로 함께
처리하면 됩니다. 심각도: 비차단·범위 밖 잔존 지적.

## Questions

없습니다.

## CI Result

오케스트레이터가 로컬 4종 검증을 재실행했습니다.

- `pnpm format:check` / `pnpm typecheck` / `pnpm lint` — 통과
- `pnpm test` — 58개 파일, 441개 테스트 전부 통과

GitHub Actions CI(Verify React Template)도 통과했습니다.

## Documentation Impact

설계 문서(`docs/designs/146-news-disclosure-split.md`)와 핸드오프
(task-066)가 구현과 일치합니다. reports 소스 제거는 설계 문서에서 확정한
사항입니다. ADR·failure record는 불필요합니다.

## Final Recommendation

Blocking 소견이 없으므로 머지 가능합니다. 변경 불가 파일(`src/shared/api/`,
다른 페이지, 라우팅)은 손대지 않았고, 외부 링크는 `target="_blank"`에
`rel="noreferrer"`가 부여되어 있습니다. 신규 테스트가 탭 전환·메타데이터
렌더·소문자 정규화·카드 실패 격리·앵커 유지를 커버합니다.

최종 머지 권한은 개발자에게 있습니다.
