# PR #257 로컬 리뷰 — 판단 기록 목록 필터 UI + 재검토 큐 (#255)

## Review Summary

판단 기록 재설계 2차 FE입니다. 1차에서 미룬 목록 필터 UI와 재검토 예정 큐를 추가합니다. 필터·큐용 BE 계약과 훅(`useDecisionLogs(filters)`·`useReviewQueue`)은 #243에서 이미 만들어져, 이 PR은 UI 위젯만 얹습니다.

`widgets/decision-filter-bar`는 `target_type`·`decision_type`·`status`·`risk_type`·`review_due_before`·`symbol` 필터를 셀렉트·입력으로 구성해 필터 상태를 만들고, 이를 `useDecisionLogs`에 전달해 목록을 갱신합니다. `widgets/review-queue-panel`은 `useReviewQueue`로 재검토 예정 목록을 받아 표시하고 항목 클릭 시 `/decision-log/:id`로 이동합니다. `decision-log-table`이 필터 상태와 연결되도록 소폭 조정됐습니다. enum 값은 `shared/model` 라벨로 표시합니다.

이번 리뷰는 필터의 쿼리 반영, enum 라벨화, 재검토 큐 소비·내비게이션을 축으로 봤습니다.

## Blocking

None.

## Suggestions

None.

## Questions

**Q1. 필터 상태가 목록 쿼리에 정확히 반영되는가.** `DecisionFilterBar`가 만든 필터가 `useDecisionLogs(filters)`의 쿼리 파라미터로 직렬화됨을 확인했고, 테스트가 필터 선택 시 파라미터 반영과 재검토 큐 항목 클릭 내비게이션을 덮습니다. 계약과 일치해 해소됐습니다.

## CI Result

Claude Code가 로컬에서 구현 커밋(`c1b6445`)과 브랜치 head에서 독립 실행했습니다.

- `pnpm format:check` — 통과 (exit 0).
- `pnpm lint` — 통과 (exit 0).
- `pnpm typecheck` — 통과 (exit 0).
- `pnpm test` — 664 passed, 80 files (exit 0). 직전(659) 대비 +5건(필터 바·재검토 큐 위젯·필터 반영·내비게이션 테스트).

GitHub Actions(FE CI: format:check+lint+typecheck+test) — PR #257에서 실행 예정. 결과는 머지 전 확인이 필요합니다.

## Documentation Impact

FE 로컬 문서 갱신은 불필요합니다. 필터·재검토 큐 계약 정본은 `project_stock`에 있습니다. ADR·Failure Record 불필요.

## Final Recommendation

머지 가능합니다. 필터 바가 6종 필터를 `useDecisionLogs` 쿼리에 반영하고, 재검토 큐가 `useReviewQueue`를 소비해 상세로 이동하며, enum은 `shared/model` 라벨로 표시함을 확인했습니다. 스코프는 필터·큐 UI에 한정돼 상세 2차(#253)·딥링크(#254)는 들어오지 않았고, BE 변경도 없습니다(계약·훅 재사용). Protected Files는 없습니다. 추가된 테스트는 필터 반영·조합·재검토 큐 렌더·항목 클릭 내비게이션·빈 상태를 덮습니다. Blocking·Suggestions는 없고 Question Q1은 리뷰 중 확인해 해소했습니다.

최종 머지 권한은 개발자에게 있습니다.
