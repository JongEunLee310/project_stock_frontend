# PR #126 로컬 리뷰 — feat: watchlist 실제 로고·AI 메모 더보기·헤더 ⓘ 툴팁·1D 스파크라인

리뷰어: Claude Code (Fable 5 — 오케스트레이터 직접 수행. VFF 세션 한도로 위임 불가)

Cross-model 리뷰: 구현자는 Codex (medium effort), 리뷰어는 Fable 5입니다. 구현에
관여하지 않은 독립 모델 시점이 확보되어 있습니다.

## Review Summary

watchlist 디테일 개선 4건(#125)을 구현하는 PR입니다. 설계 문서
(`docs/designs/125-watchlist-detail-polish.md`) Decisions §1~§4를 정확히 따릅니다.

핵심 확인 사항:

- `StockLogo`(신규, `src/shared/ui/`) — Parqet CDN URL에 KOSPI `.KS`/KOSDAQ `.KQ`
  접미사 규칙을 적용하고, `onError` 시 내부 state로 글자 마크 폴백으로 전환합니다.
  기존 `symbolMarks` 맵은 컴포넌트 내부로 이동해 페이지 중복이 없고, 폴백 마크를
  이미지 아래에 항상 렌더링해 로딩 전 빈 영역이 없습니다. `alt=""` +
  `aria-hidden`으로 장식 처리하고 종목 식별은 인접 텍스트가 담당합니다.
- AI 관찰 메모 — `note.length > 120`일 때만 `line-clamp-3` + 더보기/접기 버튼을
  노출하고, 확장 상태를 심볼 단위 `Set`으로 관리합니다. 짧은 메모는 기존과 동일하게
  렌더링됩니다.
- `InfoTooltip`(신규) — 뉴스 위험도·밸류에이션·테마 과열 3개 헤더에만 부착됩니다.
  hover/focus 표시, Escape 닫기, `role="tooltip"` + `aria-describedby`, 미표시 시
  툴팁 노드 미렌더링을 모두 충족하고 문구는 설계 문서 원문과 일치합니다. 헤더
  배열이 `{ label, info? }` 객체로 바뀌었지만 라벨 텍스트는 유지되어 기존 헤더
  단언이 그대로 통과합니다.
- 스파크라인 — `useWatchlistSparklines('1D')` 호출 변경뿐이며 queryKey는 이미
  range를 포함합니다. BE(#project_stock/240) 머지 전 환경에서는 쿼리 에러 →
  빈 맵 → 셀 `—` 폴백이 동작합니다.
- 테스트 — StockLogo(접미사 URL·onError 폴백), InfoTooltip(표시·Escape·aria 연결),
  페이지(더보기 토글·ⓘ 3개), queries(`range=1D` URL) 시나리오가 추가되었고 기존
  테스트 약화가 없습니다.

검증: `corepack pnpm format:check` / `typecheck` / `lint` / `test`(374 passed)
모두 오케스트레이터가 독립 재실행으로 확인했습니다.

## Blocking

없습니다.

## Suggestions (비차단)

- S1: `InfoTooltip`의 `aria-describedby`가 닫힘 상태에서 존재하지 않는 id를
  참조합니다. 열려 있을 때만 `aria-describedby`를 부여하면 스크린리더 동작이 더
  정확해집니다.
- S2: "새로 추가된 관심 종목" aside는 `RecentWatchlistView`에 market이 없어 한국
  종목이 항상 글자 마크로 폴백됩니다. adapter에 market을 추가하면 개선할 수 있는
  후속 여지입니다 (설계에서 의도된 제약).
- S3: 로고가 외부 CDN(Parqet) 의존입니다. 장애 시 폴백은 검증되어 있으나, 트래픽
  정책 변화 가능성은 인지해 둘 필요가 있습니다.

## Scope / Protected Files

핸드오프 범위 내 변경입니다. 보호 파일 변경 없음. 배지·필터·정렬 등 기존 동작
변경 없음. 셀 클릭 종목별 근거 팝오버는 Out of Scope(후속, BE 계약 필요)로
유지되었습니다.

## Documentation / ADR / Failure Record

- 설계 문서 Status `Implemented` 갱신 포함.
- ADR 불필요. Failure record 불필요.

## 결론

Blocking 없음. 머지 권한은 개발자에게 있습니다. 변화(1D) 스파크라인이 실제 15분
데이터를 표시하려면 BE PR project_stock#240이 dev에 머지되고 로컬 BE가 갱신되어야
합니다.

## Revision R1 재리뷰 — 요약문 클램프 (46ecbef)

개발자 피드백(메모가 여전히 길게 표시됨) 반영분입니다. 원인은 R0 설계 가정("요약문은
짧은 고정 문장") 오류로, BE `ObservationsResult.summary`에 길이 제약이 없어 요약
단락이 클램프 없이 전문 노출되고 있었습니다.

- 구현: Codex 세션이 도구 라우터 오류(`unsupported call`)로 2회 연속 실패해,
  명세가 확정된 소규모 변경(약 25라인)인 점을 고려해 오케스트레이터(Fable 5)가
  직접 구현했습니다. 이 경우 구현자와 리뷰어가 동일 모델이므로 교차 모델 리뷰
  관점은 이 개정분에 한해 확보되지 않습니다.
- 변경: `observations.summary`가 120자를 넘으면 `line-clamp-3` + 더보기/접기 토글
  (boolean state). 항목 note 로직·카드 하단 기존 버튼은 무변경. 설계 문서
  Revision R1 절과 일치합니다.
- 테스트: 긴 summary 토글 확장·축소, 짧은 summary 토글 미노출 시나리오 2건 추가.
- 검증: format:check / typecheck / lint / test(376 passed) 재실행 통과. 전체 스위트
  1회 실행에서 1건 플레이크가 관측되었으나 단독·재실행 모두 통과해 이번 변경과의
  연관은 확인되지 않았습니다.

판정: Blocking 없음.

## Revision R2 재리뷰 — 카드 단위 접기/펴기 (5145647)

개발자 피드백 반영분입니다. 문단별 클램프(R0 항목·R1 요약문)는 의도와 달라 모두
제거하고, 카드 하단에 원래 있던 "더 보기 ›" 버튼(기존에는 핸들러 없는 장식)에 토글을
연결해 메모 영역 전체를 접었다 펴는 방식으로 전환했습니다.

- 변경: 문단은 원래대로 전문 렌더링. 콘텐츠 래퍼가 접힌 상태에서
  `max-h-56 overflow-hidden`, 하단 버튼이 "더 보기 ›" ↔ "접기 ‹" 토글.
  상태는 boolean 하나로 관리합니다. 설계 문서 Revision R2 절과 일치합니다.
- 테스트: 문단 클램프 테스트 3건 제거, 카드 단위 접기/펴기 시나리오 1건 대체
  (374 tests).
- 검증: format:check / typecheck / lint / test 재실행 통과.
- 구현: Codex CLI 도구 오류가 지속되어 R1과 동일하게 오케스트레이터(Fable 5)가
  직접 구현했습니다 (교차 모델 리뷰 관점은 이 개정분에 한해 미확보).

판정: Blocking 없음.

## Revision R3 재리뷰 — 한국 종목 로고 폴백 체인·툴팁 구조화 (758b65f)

개발자 피드백 2건 반영분입니다.

- 로고: "새로 추가된 관심 종목"의 한국 종목(005930)이 market 부재로 글자 마크
  폴백되던 문제를 `StockLogo`의 후보 체인(6자리 숫자 심볼 → `.KS` → `.KQ` → 글자
  마크)으로 해결했습니다. Parqet에 `005930.KS` 실로고가 있음을 확인했고 BE 계약
  변경은 없습니다. 관심 종목 테이블의 3종(NVDA·AAPL·005930.KS)은 모두 실로고임을
  이미지 다운로드로 검증했습니다.
- 툴팁: `InfoTooltip.content`를 ReactNode로 확장하고 `MetricTooltipGuide`로
  구조화했습니다 (정의 → 판단 요소 불릿 → 실제 TableBadge 레전드 → 주의 문구).
  레전드는 셀과 동일한 resolver를 사용해 등급 값이 enum과 어긋날 수 없습니다.
  헤더 라벨 텍스트는 불변으로 기존 단언이 그대로 통과합니다.
- 테스트: StockLogo KS→KQ→글자 마크 체인, 툴팁 구조·배지 레전드 시나리오 2건 추가
  (376 tests).
- 검증: format:check / typecheck / lint / test 재실행 통과.
- 구현: Codex CLI 도구 오류가 지속되어 오케스트레이터(Fable 5)가 직접 구현했습니다.

판정: Blocking 없음.

## Revision R4 재리뷰 — 툴팁 패널 포털 배치 (커밋: feat InfoTooltip 포털 전환)

개발자 피드백 반영분입니다. 툴팁 패널이 테이블의 `overflow-x-auto` 컨테이너 내부에
absolute로 렌더링되어 열릴 때 가로 스크롤을 만들고, 스크롤하려면 호버가 풀려 내용을
읽을 수 없던 문제입니다.

- 변경: 패널을 `createPortal(document.body)` + `position: fixed`로 옮기고 버튼
  rect 기준으로 좌표를 계산합니다 (뷰포트 좌우 클램프 포함). 트리거 동작·aria
  연결·콘텐츠(R3 구조화 가이드)는 그대로입니다.
- 테스트: 포털 렌더링(document.body 직하위) 단언 추가 (376 tests).
- 검증: format:check / typecheck / lint / test 재실행 통과.
- 구현: Codex CLI 도구 오류가 지속되어 오케스트레이터(Fable 5)가 직접 구현했습니다.
- 참고: fixed 배치 특성상 툴팁이 열린 채 페이지를 스크롤하면 패널이 따라가지
  않지만, 호버 해제 시 닫히므로 실사용 영향은 없습니다.

판정: Blocking 없음.
