# PR #77 — Portfolio Day Change Wiring (feat/portfolio-day-change-wiring)

Reviewer: Claude Code (Sonnet, FE track). Implementation: Codex. Branch: feat/portfolio-day-change-wiring → main.

---

## Review Summary

All four files changed are within the design-69 scope: `dto.ts`, `adapters.ts`, `queries.ts`, and `PortfolioPage.tsx`. The DTO adds `day_change_value` / `day_change_percent` as `string | null`, the adapter maps them via `parseDecimal(...) ?? 0`, the queries fallback object is completed to satisfy the updated `PortfolioView` type, and the page adds a "일간 변동" `SummaryCard` bound to the real fields. `visual="delta"` and `positive` are pre-existing props in the `SummaryCard` type definition (`'wallet' | 'donut' | 'delta' | 'risk'`) — no new prop or visual type was introduced. Graceful degradation is confirmed: absent BE fields → `parseDecimal(undefined) ?? 0` → `0` display, with no additional guard code needed. The mock comment in PortfolioPage accurately retains only `riskExposures` and `aiBriefing` as pending mock items. Adapter tests cover the mapped-value case and the undefined-field fallback; PortfolioPage test adds label/value/percent assertions for the new card.

---

## Blocking

None.

---

## Suggestions

None.

---

## Questions

None.

---

## CI Result

Orchestrator-verified locally (provided as input):

| Check | Result |
|-------|--------|
| `pnpm lint` | PASS |
| `pnpm typecheck` | PASS |
| `pnpm format:check` | PASS |
| `TZ=UTC pnpm test` (197 tests) | PASS |
| `pnpm build` | PASS |

---

## Documentation Impact

- `docs/designs/69-portfolio-day-change-wiring.md` added (Frozen, already written by orchestrator).
- `.codex/tasks/task-026-portfolio-day-change-wiring.md` added (handoff record).
- No ADR required: consumes existing summary endpoint extension, no new architectural decision.
- No failure record required: localised change, regression covered by tests.

---

## Final Recommendation

**Approve.**

The implementation satisfies every contract item: DTO fields added, adapter mapping with `parseDecimal ?? 0` fallback, fallback object updated, "일간 변동" card added using the pre-existing `delta` visual and `positive` prop, and mock comment trimmed to reflect only the remaining pending items. Tests cover both the happy-path mapping and the missing-field-to-zero fallback. Scope items `aiBriefing` and `riskExposures` are confirmed untouched.

**최종 머지 권한은 개발자에게 있습니다.**
