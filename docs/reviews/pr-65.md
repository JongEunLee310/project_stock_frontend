# 로컬 리뷰: PR #65 — 접근성·키보드 점검 + AlertsPage 탭 갭 수정 (#20)

리뷰어: Claude Code (Opus, FE 트랙). 작성: Claude(직접, Codex 주간 사용량 소진). 판정: **머지 가능(사람 최종 승인·머지 대기)**.

## 범위 대조

`docs/designs/20-accessibility-audit.md` 점검 결과와 1:1 대조. 감사(audit) + 단일 갭 수정 성격. 코드 변경은 `AlertsPage.tsx` 탭 버튼 1곳 + 점검 문서.

## 정확성

- **점검 정확성**: 완료조건 4개를 코드 근거로 확인 — Button focus-visible, 네이티브 아이콘 버튼 aria-label/aria-expanded/aria-pressed, 입력 필드 label/htmlFor·sr-only, Badge 텍스트 동반. 단정 내용이 실제 코드와 일치. ✔
- **갭 수정**: AlertsPage 탭이 유일하게 focus-visible 누락 + 활성상태 색상 단독 → `focus-visible:outline-2/offset-2/outline-app-accent`(앱 컨벤션 일치) + `aria-pressed={activeTab===tab.key}` 추가. 토글 의미에 맞는 ARIA. ✔
- **부작용 없음**: className 추가·aria 속성만, 동작/상태 로직 무변경. 기존 탭 전환 테스트 영향 없음. ✔

## 검증 (독립 재실행)

- lint · typecheck · format:check 클린.
- TZ=UTC test **185 passed (37 files)** · build 통과(기존 청크 경고만, 비차단).

## 보호 파일

`.codex/*`·`docs/decisions/*`·`docs/harness/*` 무변경. 위반 없음.

## 문서·ADR·실패기록

- 점검 기록 `docs/designs/20-accessibility-audit.md` 신설. ADR 불요(아키텍처 결정 없음). 실패기록 불요.

## 비차단 관찰(후속, audit §4)

- 색상 대비비 WCAG AA 수치 측정은 정적 점검 불가 → 별도 후속.
- Watchlist 클릭 `<tr>` role/aria-label 부재 — 키보드 조작 가능·내부 동등 버튼 존재로 비차단.
- AlertsPage 탭 정식 `role=tablist/tab/tabpanel` 승격은 패널 구조 변경 필요 → 범위 밖.
