# PR #27 Local Review — 도메인 타입 및 Mock 데이터 정의 (task-004 / 이슈 6)

## Review Summary

task-004 핸드오프 구현 리뷰. 핵심 도메인 타입(`Stock`/`Signal`/`Portfolio`+`Holding`/`AlertRule`/`DecisionLog`)과 공통 `StockStatus`를 정의하고, 타입을 `satisfies`로 무손실 만족하는 Mock 데이터를 추가했다. 핸드오프 Goal·Implementation Scope·Test Requirements를 충족하며, Out of Scope(API 클라이언트 실제 구현·페이지 렌더링·디자인 토큰) 침범 없음. 보호 파일·의존성 추가 없음.

검토 항목:

- `StockStatus`를 `shared/model/stockStatus.ts`로 이동해 단일 소스화. 기존 `shared/ui/stockStatus.ts`는 `model`에서 re-export하여 상태 색상 매핑(`stockStatusClassNames`)과의 1:1 대응을 유지 — 이슈 3 색상 매핑과의 정합성 보존.
- 데이터 접근부(`shared/mock`)와 타입(`shared/model`)을 분리, 각각 `index.ts` 배럴로 export — 핸드오프 "실제 API 교체를 염두에 둔 export 구조" 요구 충족.
- 모호 필드(`Signal.kind`, `AlertRule.condition`)를 union 기본값으로 결정하고 PR 본문 Assumptions에 보고 — 핸드오프 "합리적 기본값 + 가정 보고" 요구 충족. `AlertRule.symbol`은 전체 시장 알림을 위해 `string | null` 허용.
- Mock은 `satisfies Stock[]` 등으로 컴파일 타임 타입 만족 — 타입 위반 시 typecheck에서 차단됨. 명시 타입 단언(`as`) 대신 `satisfies` 사용으로 리터럴 정밀도 유지.
- 정합성 단위 테스트 1건: signal symbol이 stock 집합에 속하는지, portfolio totalValue가 holdings 합과 일치하는지 검증.

## Blocking

None.

## Suggestions

None. 범위·구조·검증 모두 깔끔하며 추가 제안 없음.

## Questions

None.

## CI Result

Verify React Template — pass (27s). 로컬 검증 `pnpm lint`/`typecheck`/`test`(11 passed)/`build`/`format:check` 모두 통과.

## Documentation Impact

핸드오프 Documentation Impact(README 폴더 설명 반영)는 선택 사항이며, `shared/README.md`에 `model`·`mock` 위치 설명을 반영해 충족. 설계 기록(`docs/designs/6-domain-types-and-mock-data.md`)과의 불일치 없음(모호 필드는 설계 기록 명시대로 구현 시 결정). 추가 ADR 불필요(ADR-003 범위 내), 실패 기록 불필요.

## Final Recommendation

머지 가능 상태. 핸드오프 범위·검증 충족, 차단 이슈 없음. 자동 승인하지 않으며 최종 승인·머지는 사람이 수행한다.
