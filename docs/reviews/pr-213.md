# PR #213 로컬 리뷰 — feat: 리서치 헤더 목표주가 최고·최저 병기 — BE 컨센서스 계약 연동 (#206)

리뷰어: Claude (value-for-fable / Sonnet)

구현자: Codex(gpt-5.6-sol), 커밋 `7eaca2b`, 이슈 #206(마일스톤 #4),
핸드오프 `.codex/tasks/task-079-target-price-range.md`

## Review Summary

`git diff main...HEAD` 기준 6개 파일이 바뀝니다: 핸드오프 태스크 문서
신규 추가, `src/features/research/dto.ts`(+3), `adapters.ts`(+6),
`adapters.test.ts`(+6), `src/pages/ui/ResearchPage.tsx`(+24/-4),
`ResearchPage.test.tsx`(+17/-2)입니다.

BE 계약(`../project_stock/docs/api/frontend-api-spec.md` asset detail
절 361행)은 `target_price_high`·`target_price_low`를 nullable 문자열
Decimal로, `target_analyst_count`를 nullable 정수로 정의합니다.
`AssetDetailDto`에 추가된 세 필드의 타입이 이와 정확히 일치합니다.
어댑터(`adaptResearchDetail`)에서는 high·low에 기존 `parseDecimal`을
그대로 재사용하고, 정수 필드인 `target_analyst_count`는 파싱 없이
`?? null`로만 널 병합해 `ResearchView`에 옮깁니다 — 기존 관례를
그대로 따르는 최소 변경입니다.

표시 로직은 `ResearchPage.tsx`에 새로 추가된 `formatTargetPrice`
함수(184~199행)가 담당합니다. `low === null || high === null`일 때는
기존 포맷(`평균 (상승률)`)으로 그대로 폴백하고, 둘 다 있을 때만
`평균 (최저–최고) (상승률)` 형태로 범위를 병기합니다. 이 조건은
`high`만 있고 `low`가 없는 부분 null 케이스도 동일하게 폴백 경로로
처리하므로 값이 하나만 있는 어중간한 범위 표기(예: `–$1,300.00`처럼
한쪽이 빈 문자열)가 나올 가능성이 없습니다. 헤더 항목 자체
(`HeaderCard`의 `평균 목표주가` 라벨)는 그대로 유지되고 `value` 계산만
새 함수로 교체되어(873~882행), 기존 5개 지표 그리드 구조나 다른
헤더 항목에는 영향이 없습니다.

`target_analyst_count`는 어댑터까지만 파생되고 헤더 UI에는 노출되지
않습니다. 이슈·태스크 모두 "표기가 과밀해지면 생략 가능 — 구현에서
판단"이라고 재량을 열어뒀고, 실제로 최저·최고·상승률까지 이미 한
줄에 세 그룹이 들어가는 점을 고려하면 생략이 타당한 판단입니다
(Suggestions S2 참조).

## Blocking

없습니다.

## Suggestions

**S1: 부분 null(최고만 있고 최저가 null) 케이스의 전용 테스트 부재.**
`formatTargetPrice`의 `low === null || high === null` 조건은 코드
검토로는 두 값이 동시에 null인 경우와 한쪽만 null인 경우를 동일하게
처리함이 명확하지만, 현재 테스트 데이터(`adapters.test.ts`,
`ResearchPage.test.tsx`)는 항상 "둘 다 있음" 또는 "둘 다 null"
조합만 사용하고 한쪽만 null인 조합은 검증하지 않습니다. 리뷰 시점
요구사항이었던 "부분 null 처리"는 로직상 안전하지만, 회귀 방지
차원에서 `target_price_high`만 있고 `target_price_low`가 null인
케이스(또는 그 반대)에 대한 테스트 한 건을 추가하면 이 분기가
의도한 대로 계속 동작함을 명시적으로 보장할 수 있습니다. 현재 로직
자체는 정확하므로 블로킹은 아닙니다.

**S2: analyst_count 생략 결정의 근거가 문서화되지 않음.** 핸드오프
태스크(41~42행)는 "analyst_count의 별도 노출... 표기가 과밀해지면
생략 가능 — 구현에서 판단하고 태스크 결과에 명시"라고 요구했지만,
PR 본문·커밋 메시지·태스크 문서 어디에도 생략 판단의 근거가
기록되어 있지 않습니다. 결과물(코드)은 타당한 선택이므로 블로킹은
아니지만, 다음에 이 항목을 다시 검토할 사람을 위해 PR 본문에 한
문장 정도로 "헤더 밀도상 analyst_count는 생략" 같은 근거를 남기는
편이 좋습니다.

## Questions

없습니다.

## CI Result

GitHub Actions CI(`Verify React Template`)는 리뷰 시점 기준 `pass`
상태입니다(`gh pr checks 213`, 1m10s). 로컬에서도 corepack 경유
`pnpm` 4종 검증을 직접 재실행해 확인했습니다.

- `pnpm run format:check` — `All matched files use Prettier code
  style!` 통과
- `pnpm run typecheck` — 에러 없이 통과
- `pnpm run lint` — 에러 없이 통과
- `pnpm run test` — 60개 파일, 524개 테스트 전부 통과 (`Test Files 60
  passed (60)`, `Tests 524 passed (524)`)

PR 본문에 기재된 검증 기록(60 files, 524 tests)과 일치합니다.

## Documentation Impact

BE 계약(`project_stock#295`, PR #312)은 이미 dev에 머지되어 있고
이번 PR은 그 계약을 소비하는 FE 후속 작업입니다. `AssetDetailDto`·
`ResearchView` 타입에 필드가 추가되긴 했지만 기존 계약을 깨지 않는
순수 확장이므로 ADR 대상은 아닙니다. 실패 기록(failure record)
대상도 아닙니다. 이슈 #206이 설계 문서(`docs/reviews/pr-192.md`)에서
남겨둔 후속 항목을 정확히 닫습니다.

## Final Recommendation

Blocking이 없습니다. 머지 가능 판단입니다.

DTO 필드 타입이 BE 계약과 정확히 일치하고, `parseDecimal` 재사용으로
기존 파싱 관례를 벗어나지 않았습니다. null 폴백 로직은 부분 null
케이스까지 포함해 코드상 안전하며(S1은 테스트 커버리지 보강
제안일 뿐 로직 결함이 아닙니다), 기존 `target_price`·상승 여력
표시는 diff상 로직이 그대로 보존되어 회귀가 없습니다. CI
(`Verify React Template`)는 `pass`이고 로컬 4종 검증(format:check·
typecheck·lint·test 524개)도 모두 통과했습니다.

최종 머지 권한은 개발자에게 있습니다.
