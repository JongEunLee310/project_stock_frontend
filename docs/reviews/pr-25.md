# PR #25 Local Review — 디자인 토큰 및 공통 스타일 시스템 (task-002 / 이슈 3)

## Review Summary

task-002 핸드오프 구현 리뷰. Tailwind v4 `@theme`로 dark theme 색상·간격·폰트 토큰과 `StockStatus` 상태 색상 5종을 정의하고, `shared/ui`에 Card/Badge/Button/Input 프리미티브와 Badge 테스트를 추가했다. 핸드오프 Goal·Implementation Scope·Test Requirements를 모두 충족하며, Out of Scope(페이지·도메인 타입·차트) 침범 없음. 보호 파일 변경 없음.

검토 항목:

- 상태 색상 5종이 `stockStatus.ts`의 `StockStatus` union 및 README 매핑과 1:1 일치.
- 토큰은 의미 기반 네이밍(`status-*`, `app-*`)으로 하드코딩 색상 미사용. 위험 색상(`#3a1f24`/`#fecaca`)은 톤다운되어 과자극 아님.
- 컴포넌트는 props로 상태/변형 수신(Badge `status`, Button `variant`), `ComponentPropsWithoutRef`로 표준 속성 전달 — 최소 구성 원칙 부합.

## Blocking

None.

## Suggestions

- Prettier 포맷 불일치 4건: `src/index.css`, `src/shared/ui/Card.tsx`, `src/shared/ui/Input.tsx`, `src/shared/ui/stockStatus.ts` (`prettier --check` 기준). 현재 CI는 prettier를 실행하지 않아 차단 요인은 아니나 리포 포맷 도구와 불일치. 동일 브랜치에서 `pnpm format` 적용 후 푸시 권장(Codex 후속).
- `index.ts`가 `Card`/`Button`/`Input`은 re-export하나 `classNames`는 미노출. 의도된 내부 유틸이면 무방하나, shared 공통 유틸로 쓸 의도면 노출 검토.

## Questions

None.

## CI Result

Verify React Template — pass (19s). 로컬 검증 `pnpm lint`/`typecheck`/`test`(6 passed)/`build` 모두 통과.

## Documentation Impact

`src/shared/README.md`에 토큰·상태 색상 매핑 문서화로 핸드오프 Documentation Impact 충족. 추가 ADR·실패 기록 불필요(ADR-003 범위 내 구현).

## Final Recommendation

머지 가능 상태. 핸드오프 범위·검증 충족, 차단 이슈 없음. Prettier 포맷은 비차단 제안으로 후속 정리 가능. 자동 승인하지 않으며 최종 승인·머지는 사람이 수행한다.
