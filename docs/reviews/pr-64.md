# 로컬 리뷰: PR #64 — README 개발 가이드 갱신 (#22)

리뷰어: Claude Code (Opus, FE 트랙). 작성: Claude(직접, Codex 주간 사용량 소진). 판정: **머지 가능(사람 최종 승인·머지 대기)**.

## 범위 대조

이슈 #22 완료조건과 1:1 대조. 문서·env 템플릿만 변경, 코드 무변경.

- 실행 방법(기존 유지)·환경 변수·폴더 구조·주요 라이브러리·Mock↔API 연동 방식 5개 항목 모두 반영. ✔
- 범위 밖(코드 변경, 신규 기능) 미포함. ✔

## 정확성

- **stale 제거**: 기존 "백엔드 API 연동 전" 문구가 #48 연동 완료(PR #58/#62)와 모순 → 실제 구조로 교체. ✔
- **계층 서술 일치**: `client.ts`(envelope/errorCodes/paging/401 lazy refresh single-flight)·`auth/tokenStore`·`dto→adapters→queries`는 현재 코드와 일치(샘플 `features/dashboard/queries.ts` 확인). ✔
- **환경변수**: `VITE_API_BASE_URL`이 코드상 유일 사용처(`client.ts` 2곳)와 일치. `.env.local`은 `.gitignore`의 `*.local`로 커버됨 → `.env.example`(추적) 템플릿 추가 적절. ✔
- **참조 무결성**: `docs/api/contract-alignment.md`·`docs/knowledge/frontend-conventions.md` 모두 존재. ✔

## 검증 (독립 재실행)

- `prettier --check README.md` 통과 · `pnpm format:check`(src 게이트) 통과.
- 코드 무변경 → lint/typecheck/test 영향 없음(직전 main 검증 185 passed 유지).

## 보호 파일

`.codex/*`·`docs/decisions/*`·`docs/harness/*` 무변경. 위반 없음.

## 문서·ADR·실패기록

- 본 PR 자체가 문서 갱신. ADR 불요(아키텍처 결정 없음, 기존 구조 서술만). 실패기록 불요.

## 비차단 관찰(후속)

- `.env.example`은 prettier 파서 대상 외(무시) — CI 영향 없음.
- 후속 #20(접근성) 작업 시 README에 a11y 관련 섹션 추가 여지.
