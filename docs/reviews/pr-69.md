# 로컬 리뷰: PR #69 — 이슈 #67 P1 문서·주석 정리 + FE 이름

리뷰어: Claude Code (Opus, FE 트랙). 구현: Codex(핸드오프 task-021). 판정: **머지 가능(사람 최종 승인·머지 대기)**.

## 배경

이슈 #67 P1(env 문서·가격 시계열 주석) + FE 한정 P2(템플릿 이름). 동작 변경 없는 문서·주석·메타데이터 정리.

## 정확성

- **env 문서** — `.env.example`/README가 `/api/v1` 포함 + 로컬 예시 `http://localhost:8000/api/v1`를 명시. 클라이언트가 prefix 없는 경로로 호출하는 현 구조와 일치, 404 함정 해소. ✔
- **가격 시계열 주석** — 4곳 모두 "BE 준비됨 + 활성화 블로커(market 매핑·DTO 정렬)"로 정확히 갱신. `enabled:false`·빈 배열·DTO 타입·호출 경로 무변경 → 동작 보존. ✔
- **package.json name** — `project-stock-frontend`로 변경. `private:true` 단일 패키지라 워크스페이스/스크립트 참조 영향 없음(검증으로 확인). ✔
- 로직·쿼리·DTO 무변경. 범위 준수(차트 활성화·prefix 코드 고정·BE 레포 미포함). ✔

## 검증 (독립 재실행)

- lint · typecheck · format:check 클린.
- `TZ=UTC pnpm test` — 39 files / **188 passed** (동작 무변경, 테스트 수 동일).
- build 통과(기존 chunk-size warning만).

## 보호 파일

`.codex/*`(task 신규 추가)·`docs/decisions/*`·`docs/harness/*` 무변경. 위반 없음.

## 문서·ADR·실패기록

- 설계 기록 `docs/designs/67-p1-docs-cleanup.md` 반영. ADR·실패기록 불요.

## 비차단 관찰(후속)

- 이슈 #67 잔여: 기본 브랜치 변경(레포 설정, 사람 승인 대기), 가격 시계열 실제 활성화, API prefix 코드 고정(v0.2), 수동 smoke test(BE 구동 필요).
